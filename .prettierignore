node_modules/
_site/
originals/
generated/
package-lock.json
.github/
.claude-surf-meta.json
.claude-surf-status.json

# Prettier formats *.njk with its HTML parser (see .prettierrc.json), which
# collapses plain text onto a single line. robots.txt is line-oriented and a
# one-line robots.txt is not a valid robots.txt, so this one template opts out.
# Every other .njk is real markup and is checked normally — including
# src/sitemap.njk, whose XML the HTML printer leaves alone.
src/robots.njk

# Agent instruction file, edited by humans and tooling as prose rather than
# maintained as source. Prettier's only complaint is emphasis markers
# (*slim* -> _slim_), which is churn for zero benefit and a needless merge
# conflict surface. README.md is NOT ignored and does pass --check.
CLAUDE.md
